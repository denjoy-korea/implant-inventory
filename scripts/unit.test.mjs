/**
 * DEPRECATED: 이 파일의 extractLengthFromSize 테스트는
 * tests/unit/sizeNormalizer.test.ts 로 이전되었습니다.
 *
 * 이전 방식: TypeScript 로직을 JavaScript로 재구현 → drift 위험
 * 현재 방식: TypeScript 소스를 직접 import (vitest + @/services/sizeNormalizer)
 *
 * 실행: npm run test:unit
 */
